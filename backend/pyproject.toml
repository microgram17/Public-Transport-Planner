[project]
name = "train-tracking-backend"
version = "0.1.0"
description = "Baseline backend for a train tracking application"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<1.0",
    "asyncpg>=0.30,<1.0",
    "uvicorn[standard]>=0.34,<1.0",
]

[tool.uv]
package = false
